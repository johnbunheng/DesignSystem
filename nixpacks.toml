[phases.build]
cmds = [
    "composer install",
    "npm install",
    "npm run build",
    "php artisan migrate --force"  # បន្ថែមបន្ទាត់នេះ
]

[phases.start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"