[phases.setup]
nixPkgs = ["php82", "nodejs_22", "composer"]

[phases.build]
[phases.build]
cmds = [
    "composer install",
    "npm install",
    "npm run build",
    "php artisan migrate --force",
    "php artisan optimize"
]